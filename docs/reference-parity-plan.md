# Reference Parity Plan: ZenForge vs. deepseek-harness and codex

This document is the gap analysis behind the current upgrade wave. It maps
every capability observed in the two reference harnesses —
[deepseek-harness (DSH)](https://github.com/deepseek-ai/deepseek-harness) and
[codex](https://github.com/openai/codex) — onto ZenForge: what ZenForge already
had, what this wave implemented, and what remains as roadmap with a concrete
adoption sketch. It is a living planning artifact, not an ADR; the design
decisions themselves are recorded in `docs/adr/`.

Method: both references were inventoried capability-by-capability from their
sources (DSH `lib/` TypeScript surface, codex `codex-rs/` Rust workspace), then
each item was classified:

- **Shipped** — implemented in this upgrade wave, with tests.
- **Already had** — ZenForge had an equivalent or stronger mechanism before
  this wave.
- **Roadmap** — deliberately deferred, with a sketch of how it would land in
  the ZenForge architecture and a size estimate (S/M/L).

## Cross-cutting principles

Both references converge on the same engineering principles. ZenForge adopts
all of them; the right column points at concrete evidence.

| Principle | Meaning in the references | ZenForge evidence |
| --- | --- | --- |
| Fail-closed enforcement | Policy violations error out; unknown cases deny | `policy.ReviewCommand` denies non-allowlisted commands; `modelretry.Classify` marks unknown failures non-retryable; event-log write failures stop the run |
| Policy, not prompting | Safety lives in code paths the model cannot talk around | Tool argument validation, workspace roots, snapshot CAS, approval gates — none are prompt-level |
| Capability-boundary authorization | Crossing a boundary needs a human decision, once per boundary | Approval broker + grants store; sandbox escalation always prompts (ADR 0025) |
| Provenance on every record | Events/records carry ids, fingerprints, timestamps | Event payloads carry run/step/tool-call ids; `CompactionRecord` carries summarizer model + usage; attempt chaining via `ReplacesID`/`ReplacementID` |
| Durable marker before side effect | The log records intent before the wait/side effect | `model.retry` is emitted and checkpointed **before** the backoff sleep; approval requests checkpoint before pausing |
| Bounded resources | Every store, buffer, and loop has a cap | Attempt history 64, compaction records 64, instructions budget 32 KiB, spill 50 KiB inline, shell output caps, max steps, tool `MaxCalls` |
| Single owning writer | One component owns each mutation path | Runner owns `RunState`; checkpoint store owns persistence; adapters own provider protocol (ADR 0012) |
| Adapter-owned normalization | Core sees one typed contract | `model.Event`/`model.Message`; `model.HTTPStatusError` with `RetryAfter`; sandbox `Session`/`ExecuteResult` |
| Typed contracts | Errors and payloads are structured, not stringly | `tool.ErrInvalidArguments`, `approval.ErrRequired`, `model.StreamIdleError`, `compaction.ErrNoReduction`, typed events |
| Observability as events | Everything noteworthy is an event | ~40 event types incl. `compaction.*`, `model.retry`, `model.superseded`, `instructions.loaded` |

## Part A — Shipped in this wave

| # | Capability | Reference source | ZenForge implementation |
| --- | --- | --- | --- |
| A1 | Context compaction: pressure trigger at step boundary, prune-then-summarize, retain-boundary at tool-role edges, overflow recovery on provider context-window errors | DSH compaction package; codex `SUMMARIZATION_PROMPT`/`SUMMARY_PREFIX` + `CompactedItem` | `compaction/` package; `maybeCompact`/`runCompaction` in `agent.go`; durable `harness.CompactionRecord` (cap 64); events `compaction.started/pruned/summary/done/error`; guide: `docs/compaction-guide.md`; ADR 0023 |
| A2 | Model-call retry: failure taxonomy, exponential backoff with jitter, Retry-After honored as capped floor, attempt supersede chaining | DSH llm-retry; codex retry (200ms·2ⁿ·jitter, stream/request split) | `modelretry/` package; retry loop in `callModelDurable`; durable `model.retry` event emitted before the wait; `ModelAttempt` supersede chain; ADR 0023 |
| A3 | Stream idle watchdog | codex 300s stream idle timeout | `Config.StreamIdleTimeout` (CLI default 5m); typed `model.StreamIdleError` classified as retryable timeout |
| A4 | Empty-response retry | DSH empty_response classification | `modelretry.EmptyResponseFailure()`; supersede + retry with reason `empty_response` |
| A5 | Retry-After parsing from provider headers | codex/DSH honor 429/503 Retry-After | `model.ParseRetryAfter` (delta-seconds + HTTP-date); OpenAI/Anthropic clients capture the header into `HTTPStatusError.RetryAfter` |
| A6 | Exact-match file edit tool with ambiguous/not-found recoverable errors | DSH edit (FS_AMBIGUOUS_EDIT); codex apply_patch intent | `workspace_edit` in `tools/workspace`: exact `oldString`/`newString`, `replaceAll`, snapshot CAS guard, routed through the file-policy approval path |
| A7 | Hierarchical project instructions (AGENTS.md-compatible): root markers, root→cwd chain, override files, merged budget dropping broader scopes first, compat filenames | codex `agents_md.rs` (32 KB budget, override files); DSH agent-instructions | `instructions/` package; CLI defaults `AGENTS.override.md`/`AGENTS.md`/`ZENFORGE.md`/`CLAUDE.md`, marker `.git`, 32 KiB budget, `~/.zenforge/AGENTS.md` global; `instructions.loaded` event; ADR 0024 |
| A8 | Frozen environment context (`<environment_context>`: cwd, platform, date, mode, tools) + resume replays persisted prompting instead of rediscovering | codex `environment_context` tag + base_instructions persisted in rollout | `applyRunContext` freezes env context + rendered instructions into `RunState.Meta` (`zenforge.environment_context`, `zenforge.project_instructions`); `systemPrefixMessages` replays on resume; ADR 0024 |
| A9 | Sandbox escalation ladder: model-visible `sandboxPermissions`+`justification`, one-shot, strictly-wider, approval before spawn, denial markers | DSH bash escalation (`sandbox_permissions`, `[sandbox: …]` markers); codex denial classifier → escalate-to-approval retry | `tools/shell`: escalation args present only when confined; pairing validation; escalation never bypasses the allowlist but always prompts; namespaced escalation fingerprint rides the existing approval/grant/resume channel; approved call runs locally; ADR 0025 |
| A10 | Sandbox-denial classifier (exit 128+SIGSYS; quick-reject {2,126,127}; keyword dialects) with advisory markers | codex `is_likely_sandbox_denied`; DSH denial marker grammar | `policy.IsLikelySandboxDenied`; markers appended to confined failures with the escalation hint |
| A11 | ask_user tool: stable ids echoed in answers, structured options, multi-select, durable pause/resume, root-agent-only | DSH ask_user_question (id-echoed answers, DELEGATED_CALLER guard, non-durable waterfall) | `tools/askuser` + `approval/cli` question rendering; durable through the approval channel (operation `user.question`); answers via `approval.Decision.Payload` → new `approval.MetadataDecisionPayload`; subagents get a recoverable error; ADR 0026 |
| A12 | Tool-result spill store: 50 KiB inline cap, head/tail preview, private 0700/0600 on-disk store, fail-soft | DSH spill (50 KB cap, head/tail preview, 0700 tmp store, spill footers) | `tool.Spill` middleware; idempotent filename per run+call+arguments; CLI spills under `<workspace>/.zenforge/spill` so the read tool can reach it; ADR 0026 |
| A13 | Repeat-tool reminder at thresholds [3,5,8], escalating text, never blocking | DSH repeat-tool-reminder (consecutive identical calls, injected as plugin user message, reset on human input) | `tool.RepeatGuard` middleware, per-run streaks; deviation: reminder rides the tool result (durable in history) and does not reset on steering; ADR 0026 |
| A14 | Time/date context in the prompt | DSH time-context | `<today>` (UTC) inside the frozen environment context (A8) |
| A15 | Panic recovery + output/call bounds as composable middleware | DSH bounded tool budgets | Pre-existing `tool.RecoverPanic/MaxCalls/MaxOutputBytes/Timeout`; CLI now composes `[RecoverPanic, RepeatGuard, Spill]` as `ToolRuntime` |
| A16 | Token meter: provider rate-limit snapshots end to end + `get_context_remaining` tool | codex TokenUsageRecord/RateLimitSnapshot/`get_context_remaining`; DSH token-meter | `model.RateLimit` + `model.ParseRateLimits` (x-ratelimit-* and anthropic-ratelimit-* headers) riding `model.Usage`; durable `harness.RateLimitState` in `UsageState`; `model.ratelimits` event; the agent injects remaining tokens into tool-call metadata and `tools/contextinfo` reports them (`tokens_left` null without a configured window); ADR 0027 |
| A17 | Search discovery caps: `workspace_glob` tool + grep match caps and line previews + over-cap footers | DSH tool-fs-search (glob 100 paths newest-first with the complete list saved, grep 250 matches, 2000-byte line previews, VCS excludes) | `tools/workspace/glob.go` (`**` matcher, basename-at-any-depth patterns, List-based walk with visit budget); shared `tool.SpillStore` (extracted from the Spill middleware) wired as `SearchSpill`; ADR 0027 |
| A18 | Turn-diff tracker: per-turn unified diffs of workspace mutations | codex TurnDiff (100ms budget, path-list fallback) | `diff/` (Myers unified diff with bounded coarse fallback); `tools/workspace.TurnDiffStore` captured by Write/Edit at mutation time; the agent drains at each turn boundary under a 100ms budget into `turn.diff` events; ADR 0027 |
| A19 | WorldState diff-only environment re-injection | codex WorldState environment updates | `maybeInjectEnvironmentUpdate` re-renders live environment facts at each model-call boundary and appends an `<environment_update>` system message plus `environment.updated` event only on change; the frozen baseline (A8) is never rewritten; ADR 0027 |
| A20 | `present` tool: declare existing files as final deliverables with durable delivery records | DSH tool-present (1–8 files per call, regular-file validation, `deliverables/presented` session event) | `tools/present` validates paths through the workspace (missing files and directories rejected with DSH-style recoverable errors); the agent emits `deliverables.presented` with the tool-call id and validated files only for successful calls; ADR 0028 |
| A21 | Session titles: explicit override plus deterministic first-prompt fallback, sanitized and log-only | DSH session-title (OSC/CSI/ESC + control + bidi stripping, whitespace collapse, word and byte caps, never in the model surface) | `sessiontitle/` package mirrors `cleanTitleText`/`normalizeSessionTitle`/`fallbackSessionTitle`; `applyRunContext` freezes the title into run-state meta and the `session.title` event is published right after `run.started`; CLI `--title` and `agent.sessionTitle`; ADR 0028 |
| A22 | Observation policy completed: present-version CAS for existing files and observed-absence for new files | DSH fs-observation-policy (unseen/absent/present-version CAS) | `SnapshotStore.RecordAbsentForRun`/`AbsentObservedForRun`; `workspace_read` records absence when a read reports not-found; `workspace_write` refuses blind creates (`ErrSnapshotRequired`) and `workspace.ErrPathNotFound` now wraps `fs.ErrNotExist`; ADR 0028 |
| A23 | System-prompt registry: ordered named sections, runtime contexts, strict `{{variable}}` interpolation | DSH system-prompt (SECTION_ORDERS table, complete-section rule, `renderPrompt`/`renderContextSnapshot`, `GROUP_AT`/`VARIABLE_NAME` grammar) | `prompt/` registry with DSH order slots; persona prefix/suffix sections interpolate strictly while discovered content (environment, instructions, skills) stays verbatim; `validatePrompt` fails a run before any model request when a reference is malformed or unknown; ADR 0029 |
| A24 | Cooperative per-tool timeout declarations with a structured `TOOL_TIMEOUT` outcome | DSH tool-call-timeout-policy (`timeoutMs` on the tool definition, never model-visible) | `tool.TimeoutDeclarer` + `tool.TimeoutBudgetOf`; `tool.TimeoutPolicy` policy middleware arms the declared budget (falling back to an optional default) and replaces an expired outcome with `Metadata{code: TOOL_TIMEOUT, timeoutMs}`; the shell declares its policy maximum and the CLI composes the policy; ADR 0030 |
| A46 | Image input + reasoning replay | codex view_image + reasoning items | `model.Message.Images` carries image content beside the text; a message with images serializes as multipart (OpenAI `image_url` data URL, Anthropic base64 `image` block) while text-only messages keep the plain string form. `view_image` reads a workspace-confined path through the workspace interface (so the same confinement applies), sniffs PNG/JPEG/GIF/WebP from magic bytes rather than the extension, bounds the size, and attaches the image to the **tool result metadata** through `tools.MetadataCarrier`; the agent copies it onto the message for that tool call, so it is replayed on every later turn and survives a checkpoint (the model-visible result stays text, or the bytes would be sent twice per turn). Reasoning is captured (`model.Message.Reasoning` + `ReasoningSignature`) and stored in `MessageState.Meta`, so no run-state schema change; Anthropic replays a `thinking` block verbatim with its signature, reasoning without a signature is captured but not replayed, and OpenAI reasoning is captured but not replayed (chat completions has no accepted assistant reasoning field). Reasoning streams as its own `model.reasoning` event and never enters the answer draft. ADR 0050. Not ported: assistant-image generation, PDF/SVG, region/zoom/OCR, reasoning-effort/summary controls, OpenAI reasoning replay |
| A45 | Canned commands and schedules | DSH commands, webhook/schedule | `commands` loads `*.md` definitions from `<workspace>/`.zenforge/commands` (subdirectories are namespaces: `git/commit.md` -> `/git:commit`) with validated front matter (`description`, `argument-hint`, `model`, `agent`, `allowed-tools`, `run-bash`); an unknown key, empty body, unclosed block, non-boolean `run-bash`, or duplicate name is an error, while a missing directory is an empty catalog. Expansion is ordered so arguments cannot escalate: `@path` includes (workspace-confined, size/count bounded, word-boundary only so emails are left alone) and `!`cmd`` inline shell (only with `run-bash: true`, executed through the same shell tool and policy, left verbatim without the opt-in) run first, then `$ARGUMENTS`/`$1..$9` in a single non-rescanning pass with `$$` escaping. `--commands <dir>`, `--list-commands`, and `/name args` resolution (unknown command-looking names error with the catalog; paths and URLs pass through). `schedule` parses `every 30s|5m|5`, `@hourly|@daily|@weekly`, `@monthly` as a calendar schedule, and a five-field cron subset (`*`, numbers, ranges, lists, steps; no names, fail closed on typos); `Next` returns zero for impossible dates and the caller stops. `--schedule` repeats a task in-process, recomputing the next firing from each run's start, surviving a failed firing, and ending on cancellation. ADR 0049. Not ported: webhook-triggered runs, durable schedules across restarts, per-user command directories, interactive argument prompting, applying a command's model/agent/allowed-tools to the run |
| A44 | Independent review (guardian) | codex review/guardian process; DSH review pass | `review` runs one adversarial model call over the finished run: task, answer, changed files, unified diffs (read back from the run's own `turn.diff` events, so no new bookkeeping), commands, and failures, all bounded. A verdict is strict JSON (`approve`/`request_changes`/`comment` + findings with severity/path/line/issue/suggestion); fenced output is tolerated, findings with no issue are dropped, and an unknown decision/severity is an **error** — a reviewer that failed to answer is never read as approval. `--review off|report|enforce`: report records a `review.completed` event and finishes (default), enforce turns `request_changes` into a refusal at the stop boundary with the findings as the agent's next instruction, sharing the Stop hook's 3-refusal bound so a reviewer cannot loop a run. The reviewer runs after the hooks (a hook refusal already forces another turn), and a broken/timed-out reviewer is inert: it can neither block nor approve. ADR 0048. Not ported: adjudication of repeated findings, post-review messages, severity escalation, PR/UI attachment |
| A43 | Durable cross-run memories | codex memories (read/write crates, stage-one + consolidation prompts) | `memory` keeps learnings in two human-readable files: `raw_memories.md` (append-only blocks) and `memory_summary.md` (the injected form, rendered deterministically from the raw file within a byte budget and entry cap). Identity is the content (`memory.ID(scope, project, text)`), so the same learning found twice is one memory, and scope/project decide visibility (a project sees its own plus every user memory; an unattributed project entry is shown nowhere). `ModelDistiller` makes one optional model call with a prompt adapted from the reference's stage-one extraction (bounded task/result/commands/failures/files/event-trail input; "prefer saying nothing over guessing"; JSON output, fenced blocks tolerated, malformed output is an error). The summary is frozen into durable `RunState.Meta` (`zenforge.memory_context`) and rendered as a system section (`prompt.OrderMemoryContext`, next to hook context), so a resume replays it; a summary that cannot be read fails the run **before the model is called**, while a distillation or write failure at run end is reported as a `memory.recorded` event and never costs the user their answer. `--memory <dir>`, `--memory-scope user|project`, `--memory-distill`. ADR 0047. Not ported: rollout batches/leases/retries, usage telemetry, citation parsing of later output, extension pruning |
| A42 | Lifecycle hooks in the agent loop | codex `hooks` events (`user_prompt_submit`, `session_start`, `stop`) | `SessionStart` and `UserPromptSubmit` run once in `applyRunContext`; their `additionalContext` is frozen into durable `RunState.Meta` under `zenforge.hook_context` and rendered as a system section between the project rules and the skill catalog, so a resume replays what the original run saw instead of letting an edited script change a run in flight. A block or `continue:false` on either event fails the run with the hook's reason **before any model call**, and is reported as a `hook.completed` event. `Stop` is wired through `harness.Runner.StopHook` at all three terminal paths (resumed finalize, text-answer completion, tool-use-limit final answer): a refusal appends the reason as the agent's newest instruction, emits `stop.blocked` with the attempt number, and re-enters the loop, bounded at three refusals per terminal path so a hook that never relents cannot hold the agent hostage. Agent-level tests use real hook scripts: context reaching the system prompt, a refusal before any model request, a forced second turn with the reason visible to the model, the retry bound, and no hook work when no hooks are configured. ADR 0045 |
| A41 | Hooks engine | codex `hooks` crate (`schema.rs`, `engine/output_parser.rs`, `events/pre_tool_use.rs`) | `hooks` runs user commands at `SessionStart`/`UserPromptSubmit`/`PreToolUse`/`PostToolUse`/`Stop` with the reference's contract: a JSON payload on stdin, exit 0 parses stdout as a JSON decision (`continue`/`stopReason`/`systemMessage`/`suppressOutput`/`decision`+`reason`/`additionalContext`/`updatedInput`), exit 2 blocks with stderr as the reason, any other code fails. A malformed decision (non-JSON, unknown decision, block without a reason, bare `exit 2` with no stderr) is a **failure, never an opinion**, so a hook that meant to block is not ignored for a formatting mistake; failures are recorded in result metadata and fail open unless that hook sets `failClosed`, which also covers a timeout. Matchers are regexes matched against the tool name and only allowed on the tool events; an invalid matcher is an error rather than a silent skip. The payload carries both snake_case and camelCase keys, and event names accept either convention. `hooks.Middleware` wires `PreToolUse`/`PostToolUse` around every tool call (block before the tool runs, rewrite `updatedInput`, annotate results, block after the fact), and `--hooks <file>` loads a strict config file (unknown fields rejected, validated before anything else is constructed). ADR 0044 |
| A40 | Model-facing job tools | codex `exec_command`/`write_stdin`; DSH job tools | `tools/jobs` exposes `exec_command` (foreground with a bounded timeout, or `background=true`), `write_stdin` (input plus optional close), `job_output` (offset-based reads with `waitMs` long-polling, next offsets, dropped-output flags, exit code, and a hint explaining how to wait when a job is still running), `job_list`, and `job_kill`. Unknown ids map to `ErrInvalidArguments` with a pointer to `job_list`; `job_list` is declared read-only so plan mode allows it while the four state-changing tools stay refused. Registered with `--jobs` (or `agent.jobs`); the manager is closed when the command's context ends, so an interrupted CLI does not leave a dev server behind. Tests drive the full lifecycle through the tool interface: foreground exit codes and rendering, background start then offset polling to exact output, stdin feeding and close, dropped-output reporting, list/kill, and every invalid-argument path. |
| A39 | Background jobs with offset-addressed output | codex `exec_command`/`write_stdin`; the harness job tools | `jobs` manages long-running commands: `Start` launches a detached job (its context is `context.WithoutCancel`, so it outlives the tool call), `Run` is the foreground form that honours caller cancellation by killing and reaping, `Output(since, max)` reads each stream from an absolute offset and reports `Dropped` when the bounded ring buffer discarded bytes, `Write`/`CloseStdin` feed interactive programs, `Kill`/timeouts stop them, and `List`/`Get` snapshot status, exit code, pid, byte totals and duration. The job limit fails loudly instead of queueing, so the model is never told a command runs when it does not. Tests cover buffer offsets across drops, polling without re-reading, kill, timeout, stdin, the limit, dropped-byte reporting, cancellation, and unknown ids. ADR 0043 ; a poll always carries `stdout`/`stderr`, even when it found no new bytes, because omitting an empty field makes "nothing yet" indistinguishable from a response shape the client does not understand (a blind assertion on it panicked CI on a slower machine, and the invariant is now pinned by a test) |
| A38 | Restrict-then-exec helper binding Landlock + seccomp | codex `codex-linux-sandbox` argv0 helper | `sandbox/linuxsandbox` turns the two in-process layers into a `sandbox.Sandbox`: for each command the adapter spawns `zenforge linux-sandbox --policy <json> -- <shell> -c <command>`, and the hidden subcommand decodes the policy (rejecting unknown fields, so a policy the helper does not fully understand is an error rather than a partially applied sandbox), probes the Landlock ABI, plans **both** layers before applying either (a policy Landlock cannot express fails while the process is still unrestricted), then applies Landlock, applies seccomp, and execs. The policy JSON and its SHA-256 ride the session metadata. Fail-closed on off-platform hosts, a missing helper, or a protected-name policy (refused at construction, not at the first command). The adapter's helper-argument shape and every error mapping are unit tested off Linux through an injectable runner; a helper-process test exercises the real Landlock and seccomp system calls on Linux. `--sandbox landlock` selects it. ADR 0042 |
| A37 | Seccomp network filter | codex `linux-sandbox/src/landlock.rs` (seccomp section) | `sandbox/seccomp` plans the classic BPF program as data on any platform and applies it on Linux: an architecture guard that kills a foreign ABI (without it the wrong syscall table silently fails open), unconditional denies for the network syscalls, `ptrace`/`process_vm_readv`/`process_vm_writev`, and `io_uring_setup/enter/register` (io_uring can create sockets without `socket(2)`), a domain check that keeps `AF_UNIX` and denies every other domain for `socket`/`socketpair` (`AF_UNIX` means `socketpair(2)`, the capability subprocess tooling needs; a unix *listener* stays denied because `bind`/`listen`/`connect` are denied unconditionally), `recvfrom` deliberately allowed because subprocess tooling needs a Unix socketpair, a trailing deny return with the real Linux `EPERM` (1, pinned by a test: the constant used to hold 22, which is `EINVAL`, so a refusal reported "invalid argument"), amd64/arm64 tables only (a hand-copied table for an architecture that cannot be cross-compiled is a silent hole) plus a numeric `ExtraDeny` escape hatch, and a loud failure when a jump would exceed classic BPF's 8-bit offset. A test-local BPF interpreter verifies the program's semantics (allow/deny/kill, `AF_UNIX` vs `AF_INET`, cross-architecture, and the exact `SECCOMP_RET_ERRNO|EPERM` return) rather than only its shape, both hand-written syscall tables are restated independently in a test and compared in both directions, and on Linux the running architecture's table plus the audit arch are cross-checked against `golang.org/x/sys/unix` (ADR 0052), and the on-kernel denial test makes the syscall from a re-exec'ed test binary instead of a shell (`/dev/tcp` is a bash extension, so on a dash-based host the old check could pass without ever calling `socket(2)`); `Available` probes `/proc/sys/kernel/seccomp/actions_avail` because a filter cannot be uninstalled and probing by installing one would permanently restrict the agent. ADR 0041 |
| A36 | Landlock planner + Linux applier | codex `linux-sandbox/src/landlock.rs` | `sandbox/landlock` splits the ABI-aware access arithmetic from the syscalls: the planner computes `EXECUTE/READ_FILE/READ_DIR` read access (whole filesystem by default, or only the declared read roots when `FullDiskRead` is false), full read-write on the writable roots and on named read-write paths such as `/dev/null` (which is also granted by default, because a sandbox that denies writing to the null device breaks ordinary `cmd >/dev/null` shell use), and a `HandledAccess` mask covering every right the probed ABI supports, since an unhandled right is unrestricted rather than denied. Rights are derived per ABI (`REFER` from 2, `TRUNCATE` from 3, `IOCTL_DEV` from 5) and a newer kernel is clamped to the newest known ABI. Rules for non-directories are masked down to the rights the kernel accepts on a file (`EXECUTE`/`READ_FILE`/`WRITE_FILE`, plus `TRUNCATE` and `IOCTL_DEV` by ABI): the kernel rejects a path-beneath rule on a file that carries directory rights with `EINVAL`, which failed the whole ruleset and stopped the helper from running anything — caught in CI on Linux, since the planner compiles everywhere but only a Linux kernel can reject the rule (ADR 0046). A policy that asks for protected names inside writable roots is rejected with `ErrUnsupportedCarveOut` — Landlock unions matching rules and has no deny rule, so the carve-out is inexpressible and silently ignoring it would grant more than promised. The applier (build-tagged) performs `landlock_create_ruleset`/`add_rule`/`restrict_self` with `PR_SET_NO_NEW_PRIVS` and `Exec`; off Linux the same API reports `ErrUnsupported`. Binding it to the shell tool needs a helper process that restricts and then execs (the reference's `codex-linux-sandbox`), recorded as remaining work. ADR 0040 ; every access bit is restated in a test and cross-checked against `golang.org/x/sys/unix`'s `LANDLOCK_ACCESS_FS_*` on Linux, and the minimum-ABI column is checked for distinctness, coverage of all 16 rights, and the right generation (2 for `REFER`, 3 for `TRUNCATE`, 5 for `IOCTL_DEV`) (ADR 0052) |
| A35 | Sandbox wiring in the CLI | codex sandbox configuration + the existing ZenForge shell escalation ladder | `--sandbox <none\|seatbelt\|bwrap\|docker>` (or `shell.sandbox.backend`) builds the backend and registers it on the shell tool with `KeepSessionOpen`, so every command of a run reuses one layout and the sandbox state rides the checkpointed tool metadata; `--sandbox-root`, `--sandbox-allow-network`, `--sandbox-restricted`, `--sandbox-image`, `--sandbox-protected`, and `--sandbox-timeout` cover the details, an unknown backend is a usage error, and an unavailable backend surfaces `sandbox_unavailable` instead of executing unsandboxed. Found and fixed in the same pass: `--plan`, `--goals`, and `--goal-max-rounds` were documented and config-wired but never bound as flags. |
| A34 | Linux bubblewrap sandbox backend | codex `linux-sandbox/src/bwrap.rs` | `sandbox/bwrap` builds the sandbox as an argument list, so the policy is auditable and testable off Linux: `--new-session --die-with-parent`, a read-only host root (or a tmpfs root plus the approved read roots and the ported platform defaults when `FullDiskRead` is false), `--dev /dev`, `--bind-try /dev/shm`, writable roots bound shallowest-first (a nested root wins over its parent's protections), `--ro-bind-try` for explicit read-only paths, `--ro-bind` over existing protected metadata (`.git`, `.zenforge`), `--unshare-user/pid/ipc`, `--cap-drop ALL`, `--unshare-net` unless network is allowed, a fresh `--proc`, and `--chdir` into the canonical working directory. Paths are canonicalized, missing writable roots are dropped (bubblewrap cannot bind a missing target) and missing protected paths are skipped (documented: bubblewrap cannot mask a missing target). Seccomp filtering is not applied — the reference installs it through a separate helper process (see A35 for the remaining work). ADR 0039 |
| A33 | macOS Seatbelt sandbox backend | codex `sandboxing/seatbelt.rs` (+ `seatbelt_base_policy.sbpl`, read-only platform defaults, network policy) | `sandbox/seatbelt` implements the existing `sandbox.Sandbox` interface with an SBPL profile generated per session. The base policy, platform read-only defaults, and network policy are ported verbatim (a reworded rule is a different rule to the kernel's policy compiler); on top of them the builder adds caller-declared read roots, writable roots, and an explicit read-only set. Protection of `.git` and `.zenforge` inside a writable root is expressed as an exclusion on the write **allow** rule, not as a deny with `require-not`: a deny there matches the complement and would deny the whole root (a bug the real-enforcement test caught). Paths are canonicalized (`EvalSymlinks`, longest-existing-ancestor fallback) because the kernel matches resolved vnodes: `/tmp` is not `/private/tmp`. Each writable root is also readable and its `path-ancestors` are readable, so tools can resolve their working directory. Paths travel as `-D` parameters rather than interpolated text, so a hostile path cannot rewrite the policy. Network is denied unless requested; the adapter refuses to run at all off darwin or without the runner. Verified against the real kernel policy compiler on darwin (writes inside allowed, outside denied, `.git` protected, network unreachable). ADR 0038 |
| A32 | Goals driver and Ralph fresh-agent loops | DSH goals domain (`dsh-goal`), goal+ralph tools and the round driver | `goals/` ports the goal domain: a revision-checked lifecycle (`create`, `edit`, `pause`, `resume`, `complete`, `block`) with the reference's exact transition rules (a create must be a fresh active revision-one goal with zero rounds; every later operation advances the revision by exactly one while preserving the counters and timestamps; an id may never be reused), a round budget with in-order admission, and the `blockedReason{code,message}` shape. The rule that a goal may only be reported blocked after the same condition persists for `MinBlockedRounds` (3) consecutive rounds lives in the lifecycle and counts at most once per round, so repeating the tool inside one round cannot manufacture evidence. `goals.Store` has memory and atomic file implementations keyed by session; `tools/goal` exposes `create_goal`, `get_goal`, and `update_goal`. `RunGoal` drives a persisted goal round by round (each round admitted and checkpointed), and `RunRalph` implements the reference's fresh-agent loop with the bounded structured report (`status`/`summary`/`evidence`/`nextSteps`/`blocker`, validated exactly, capped by `maxHandoffChars`). CLI: `--goals`/`agent.goals` register the tools, `zenforge goal` drives rounds by resuming the same run (the model owns the lifecycle through the tools), and `zenforge ralph` runs fresh runs over the shared workspace, persisting each round report. ADR 0037 |
| A31 | Plan mode: read-only until the plan is approved, with a durable phase switch | codex plan collaboration mode + `exit_plan_mode`; DSH plan mode | `tool.ReadOnlyDeclarer`/`ReadOnlyOf` (fail-closed: an undeclared tool is mutating) plus the `tool.PlanMode` middleware, which refuses mutating calls with a structured `PLAN_MODE_READ_ONLY` result and guidance; read-only declarations added to the workspace read/list/glob/grep tools, todo, ask_user, present, get_context_remaining, tool_search, and web tools. `tools/plan`'s `exit_plan_mode` routes the plan text through the approval broker, and its approval must match the plan's own fingerprint (the generic rule-key fallback would let one plan authorize another). The phase lives in run-state Meta (`zenforge.plan.mode`), so it is checkpointed; the agent sets planning once (a resumed run keeps its phase) and, on an approved plan, switches to executing and emits `plan.approved`. CLI: `--plan` and `agent.planMode`, ADR 0036 |
| A30 | Run time travel: checkpoint fork and append-only revert over monotonic ordinals | codex rollout (ordinals, fork/revert, writer lock); DSH session v3 (sequence + projections) | The JSONL event store already enforced contiguous per-run ordinals under an in-process mutex plus a cross-process `flock`; appends and tail reads now use a size-validated tail cache so they no longer rescan the log, and two tests cover the cache across store instances and concurrent writers. `checkpoint.HistoricalStore`/`LoadAt` add time travel to the memory, JSONL, and SQLite stores. `Agent.Fork` starts a child run from the newest checkpoint at or below a sequence: the child's first persisted event is its own `run.started` (so the log invariant holds), and lineage lives in `ParentRunID` plus `zenforge.fork.*` meta. `RevertRun`/`Agent.Revert` append a `run.reverted` marker and save the rewound state as the run's newest checkpoint, so nothing is truncated; `resume --revert-to` continues from it and `fork`/`revert` are new CLI commands (revert needs no model). DSH's FTS/projection/migration layer is not ported; ADR 0035 |
| A29 | Web tools with an SSRF-safe fetch transport | DSH `web_search`/`web_fetch` + `dsh-web-fetch-http` (resolve once and validate the whole answer set, pin the connection against DNS rebinding, same-origin redirects only, byte/char caps, `WEB_*` error codes, untrusted-content notice, 1-4 queries, 8 results) | `web/` is the transport (`Policy` with an unexported-defaults resolver seam, `ResolvePublic`, address-pinning `DialContext`, pre-network URL policy, explicit non-reachable range classification incl. IPv4-mapped/6to4/Teredo/NAT64, content-type classification, truncation) plus the tag-stripping HTML→text converter; `tools/web` builds `web_search` (pluggable `Searcher`, bound-then-collapse query rules, URL-deduplicated capped sources, `HTTPSearcher` for generic/Brave JSON) and `web_fetch` (reference header + notice + reserved truncation footer, optional approval per call); both register only when `web.enabled` or a search endpoint is configured; ADR 0034 |
| A28 | `apply_patch` envelope: one tool that adds, updates, moves, and deletes files | codex apply-patch (Lark grammar, `*** Begin Patch` envelope, `seek_sequence` matching with Unicode-punctuation normalization, git-style `A`/`M`/`D` summary, Add overwrites an existing file) | `applypatch/` parser + applier ported with the reference's leniency (whitespace-tolerant markers, `<<'EOF'` heredoc unwrapping, line-numbered hunk diagnostics) and its four-pass context search; `tools/patch` wraps it with the workspace file policy (one approval whose fingerprint covers the whole patch), the same-run observation policy (read before update, observed absence before create), optional `workspace.Deleter` for deletes/moves, turn-diff capture per path, and the `Success. Updated the following files:` summary; ADR 0033 |
| A27 | Layered configuration with managed requirements, profiles, strict fields, and redacted secrets | codex `ConfigLayerStack` precedence (`PackagedDefaults` < MDM/system < user < user+profile < project < session flags), `requirements.toml` constraints (`ConstraintError` validator + normalizer), `profiles.<name>`, `strict-config`, `utils/redacted-string` | `configlayer/` stack (system 10 < user 20 < profile 21 < project 25 < `--config` 27 < flags 30, deep merge with per-leaf provenance, dotted `allowed`/`enforce` requirements applied after merging, profile applied directly above its defining layer, shape-conflict and unknown-field rejection); `redact.String` redacts `%v`/`%s`/`%#v`/slog and stays JSON-transparent; `--profile`, `--requirements`, `--strict-config`, `--ignore-user-config`, inline `model.apiKey`; ADR 0032 |
| A26 | Headless `exec` protocol: `--json` JSONL event stream, `--output-schema` structured final output, `-o/--output-last-message` | codex `exec --json`/`--output-schema`/`--output-last-message` (prompt arg, `-` or piped stdin) | New `exec` command sharing the run machinery: events print as one `zenforge.Event` JSON object per line (same shape as `events --json`), the schema is forwarded as a strict provider `json_schema` response format labelled with the file stem, and the last message is written to a file; Anthropic fails loudly with `model.ErrUnsupportedOutputSchema` because the Messages API cannot enforce a schema; ADR 0031 |
| A25 | Deferred tool loading (`tool_search`): large catalogs stay out of the prompt until searched | codex `tool_search` + `defer_loading` (default limit 8, loadable results) | `tool.DeferredTool` marker + `MemoryRegistry.DefinitionsMatching`; `tools/toolsearch` searches deferred names/descriptions and returns capped matches; the agent activates matches into durable `zenforge.active_tools` and emits `tools.activated`, and `toolSpecs(state)` hides unactivated definitions; MCP gains `ToolsDeferred`; ADR 0030 |

## Part B — Already had (parity confirmed by this review)

| Capability | Reference analogue | ZenForge mechanism |
| --- | --- | --- |
| Durable runs: checkpoint every boundary, crash-safe resume, terminal replay | codex rollout JSONL (ordinals, writer lock, fork/revert); DSH session format v3 | `checkpoint/` (JSONL + SQLite), `harness.RunState` version `zenforge.run_state.v1`, fail-closed saves, resume tests across model/tool/approval boundaries |
| Append-only event log, fail-closed writes, multi-sink | DSH session format (58 event types, projections, SQLite FTS) | `eventlog/` (memory/JSONL/SQLite) + `Bus`/`FanoutStore`; ~40 typed events; `run.error` never emitted for cancellation |
| Approval brokers, durable inboxes, cross-run grants | codex session approval cache + durable execpolicy amendments; DSH permission presets | `approval/` brokers (CLI/always/deny/store), `PendingBroker`, memory/SQLite inboxes + `GrantStore` with fingerprint/rule scopes |
| Model-attempt lifecycle with supersede chaining and history cap | codex StreamError events + retry accounting | `ModelAttempt` Started→Streaming→Committed/Interrupted/Superseded, `ReplacesID`/`ReplacementID`, cap 64 |
| Deny-by-default shell with AST-level review | codex execpolicy + ParsedCommand classification; DSH bash budgets | `policy.ReviewCommand` + `safety/bashast`/`safety/bashsec` (wrapper commands, redirections, embedded scripts); ADR 0010 |
| Contained shell execution | codex Seatbelt/bwrap; DSH fs sandbox modes | `sandbox/` adapters (docker/fake/containerhub), no silent fallback (ADR 0019/0020); session reuse via metadata; OS-level modes are roadmap (C5) |
| Workspace tools with observation policy | DSH fs tools + observation policy (unseen/absent/present-version CAS); codex apply_patch per-path checks | `tools/workspace` read/list/grep/write/edit; `RequireReadBeforeWrite` + `SnapshotStore` SHA256 CAS; read/write roots; byte caps; symlink-escape rejection; `workspace.changed` events |
| Subagent orchestration with depth + fan-out limits | DSH subagents (depth 1, 1–16 children); codex collaboration modes | `subagent` runtime + `task`/`agent_invoke` tools; child checkpoints; cancelled children propagate as failures; ADR 0005/0017/0018 |
| Planner/todo + plan-execute preset | codex update_plan + plan mode; DSH todo | `planner` + `PlanningMode` presets; durable plan/execute stages; ADR 0013/0014 |
| Skills with progressive disclosure | DSH skills (layered providers, digest-gated catalog); codex skills ($mentions, implicit detection) | `skill` bundles + catalog prompt; ADR 0022 |
| MCP client integration | DSH MCP client (startup timeout, namespacing, elicitation); codex MCP (namespaces, read-only auto-approve) | `adapters/mcp` client: `ToolsWithOptions` namespaces every remote tool as `mcp__<server>__<tool>`, sanitizes names onto the tool-name grammar inside the 64-character limit, reads `readOnlyHint`/`destructiveHint`/`openWorldHint` into the definition, declares a per-server tool-call budget, and gates any call the server did not declare read-only behind the approval broker (rule key `mcp:<server>:<tool>`, fingerprint over the arguments) — ADR 0054/0055. The CLI starts the stdio servers its `mcpServers` section names before the deferred-tool decision, owns each process through a per-command drain, and fails the command when a server cannot start. Elicitation, sampling, resources, and prompts remain unported |
| HTTP/SSE server + run manager | DSH api/SDK/headless (thread.* JSONL); codex `exec --json` | `server/` + RunManager; access-control hook |
| Provider streaming adapters owning protocol details | codex model metadata + transport fallback; DSH adapter-owned normalization | `model/openai`, `model/anthropic` streaming; typed `HTTPStatusError` |
| Tracing/OTel as best-effort observability | DSH observability-as-events | `trace/` + otel exporter that cannot change run results |
| Layered CLI configuration with generated reference | codex layered config (user > project) | `zenforge init` defaults; `docs/config-reference.md` verified against generated defaults; profiles/admin layer are roadmap (C15) |

## Part C — Roadmap (deferred, with adoption sketches)

Deferred items are real gaps, sequenced by value-per-effort against the
ZenForge architecture. None blocks the shipped surface above.

Status when this line was written: C3 (hooks, A42/ADR 0048), C23 (memories,
A43/ADR 0047), C13 (guardian review, A44), C19 (commands and schedules,
A45/ADR 0049), C16 (images and reasoning, A46/ADR 0050), C20's MCP server,
framing, namespacing and `zenforge mcp-server` halves (ADR 0053), and C20's
client side — configured `mcpServers`, namespaced tools registered with the
agent, approval gating by the server's own hints, command-owned process
lifecycle, and an id-dispatched stdio transport whose deadlines are real
(ADR 0054 and ADR 0055) — and C20's **run-starting tool** (`--allow-run`,
`zenforge_run`, and the served-run approval downgrade, ADR 0056) — and C9's
**engine half** (`workflow/`: the goja runtime, hook semantics, caps,
cancellation, and the schema subset validator, ADR 0057) — and C9's
**wiring half** (the `workflow` tool, `agent.go` routing, and a child runner
over the sub-agent orchestrator, ADR 0059) — and C2's **terminal sessions and
head+tail buffers** (ADR 0060) have since shipped, so C9 and C2 are complete
and what is otherwise genuinely still open is the gaps each ADR
records under its Alternatives/Consequences section (webhook-triggered runs,
durable schedules across restarts, per-user command directories, MCP
resources/prompts/elicitation, per-server MCP timeouts, a detached-run
registry for a caller that cannot wait, and the deliberately unported parts of
each capability). The rows below are kept as the original sketches so a later
reader can see what was planned against what was built.

| # | Capability | Reference source | Adoption sketch | Size |
| --- | --- | --- | --- | --- |
| C2 | Persistent PTY shell sessions (`unified_exec`: session ids, stdin writes, yield windows, head+tail buffers, proc caps, credential-scrubbed snapshots) + background jobs registry | codex unified_exec; DSH jobs/bash run_in_background | **Shipped (ADR 0060), on the existing `jobs/` registry rather than a new `tools/exec`.** The registry half was already there and is unchanged: `jobs/` assigns session ids (`job_N`), keeps stdin writable through `write_stdin`/`CloseStdin`, answers offset-addressed yield windows through `job_output` (`Output(id, since, max)` with absolute monotonic offsets), bounds each stream and the number of concurrent jobs, applies a timeout, and stops a job with `job_kill` — and since ADR 0058 a job becomes terminal only after its output is drained. This batch adds the two things that were genuinely missing. **Terminal sessions:** `Spec.PTY` runs the command on a pseudo-terminal (`creack/pty`, default 24x80, `rows`/`cols` validated and refused without `pty`), the job's stdout carries the merged stream while stderr stays empty and zero-total (a terminal has one stream), a `TERM` is injected only when the resolved environment has none, writing to the master *is* writing to the session's stdin, and the master's `EIO` after the child leaves is the drain signal — so a session is started, driven, read, and stopped with the same five tools. A PTY child is a session leader, so `kill(-pid)` signals its whole process group: killing only the shell would leave a `SIGHUP`-ignoring foreground child holding the terminal open until `DrainGrace` expired (the test fails when the group kill is removed), and the Unix-only signal is a documented no-op elsewhere. **Head+tail buffers:** a bounded `Buffer` now retains the first bytes of a stream (`min(capacity/4, 8KiB)`) as well as its newest, keeps offsets absolute, and reports `Chunk.Elided` for the discarded middle, so a flooded job still shows how it started without ever handing a reader a hole it cannot see; `job_output` surfaces `elidedBytes` and an `output-dropped(N)` footer. **Deliberate non-port:** credential-scrubbed snapshots. No view, event, or tool output carries a job's environment (`Spec.Env` is `json:"-"`), so there is no snapshot surface to scrub and a scrubber would be a helper with no caller; the reason is recorded in ADR 0060 instead of shipped as dead code. | L |
| C3 | Hooks protocol/engine (lifecycle events, matchers, command/MCP handlers, exit-2 block, deny>ask>allow, managed-only lockdown) | DSH hooks (claude-code/codex dialects); codex hooks engine (12 events) | `hooks/` package with a typed event enum, matcher config, and a broker that can veto tool calls before the invoker; compose as outermost middleware | L |

| C9 | Workflow engine (JS orchestration script, agent()/pipeline()/parallel() hooks, schema-validated results) | DSH workflow tool | **Engine shipped (ADR 0057).** `workflow/` embeds goja and runs the reference's script contract in-process: the body is wrapped in an async function, so top-level `await` and `return <value>` are legal, and the script sees `agent(prompt, opts?)`, `parallel(thunks)`, `pipeline(items, ...stages)` (no barrier between stages, stage called as `stage(previous, item, index)`), `phase(title)`, `log(message)`, and `args` (parsed into a fresh value, so a script cannot mutate the caller's bytes). `agent()` resolves to the child's final text, or to the object behind `opts.schema` when one was requested, and to `null` when the child did not complete; `opts` accepts exactly `label`/`phase`/`schema`/`provider`/`model`, refuses the reference's deferred names (`effort`/`isolation`/`agentType`) as deferred and everything else as unrecognized, and a requested schema with nothing structured behind it is a failed item rather than a value. Fatal and non-fatal are separated by a marker rather than by message text: engine failures are thrown as an `Error` carrying `code` and `fatal: true`, so an ordinary throw inside a thunk or a stage nulls that item (and skips its remaining stages) while a misused hook, unsupported option or schema, tripped cap, or cancellation rejects the combinator's promise — which the script may still catch — and a marked failure that reaches the top level uncaught keeps its code in the reported error. Children queue beyond `MaxConcurrentAgents` and start in call order as slots free, `MaxTotalAgents` (1000) stops a runaway loop and reports how many children actually started, `MaxItemsPerCall` (4096) bounds one combinator call, and the engine's child context is always cancelled when the run ends. Cancellation is the next hook boundary (every hook throws `CANCELLED`, waiting `agent()` calls are rejected rather than nulled), backed by a `CancelGrace` timer plus a last-resort runtime interrupt armed with the same window, so a script parked on a promise no hook owns cannot hang the run and cancellation never races the cooperative path. `ValidateObjectSchema` enforces the reference's subset exactly (object-rooted type/oneOf/properties/required/additionalProperties/items/enum/const plus description/title/default/examples annotations, reporting every violation at once), and the script's return value is serialized through the runtime's own `JSON.stringify` and re-validated as JSON, so a function or a cycle is `RESULT_UNSERIALIZABLE`. The taxonomy is the reference's plus two documented Go-side additions: `SCRIPT_ERROR` (the script threw) and `SCRIPT_TIMEOUT` (the synchronous prefix outlived `SyncTimeout`). Children sit behind a `Runner`/`Child` seam (`StartChild` then `Result`, so a start failure is `AGENT_START` and an unobservable outcome is `AGENT_RESULT`; `StartFunc` adapts a plain function). **Wiring shipped (ADR 0059):** the `workflow` tool (`tools/workflow`) is intercepted in `invokeToolOrRuntime` like `task` and advertised wherever sub-agents are configured; each `agent()` call is a one-task sub-agent run through the existing orchestrator (same run/step/tool-call identity, nesting depth, options merge, and live subtask events), with `Config.WorkflowAgent` (or the first registered sub-agent) choosing the worker and `Config.WorkflowLimits` bounding the run. A completed run renders the reference's text shape plus structured `{workflow, agentsStarted, stopReason, value}`, and a failed one carries the engine's own error text with its code in structured content. `opts.provider`/`opts.model` are *refused* by this host with a fatal `AGENT_START` rather than silently ignored (no per-child model resolver exists yet), a `schema` child is asked for one JSON object and its parsed answer is the structured result (a non-JSON answer is a failed item, i.e. `null`, not a fatal error), `phase()`/`log()` ride `subtask.event` with workflow-tagged types, and workflow children deliberately stay out of the parent's run-state subtask plan (resume replays the script; children are reused through their deterministic `<toolCallId>_agent_<n>` ids and existing checkpoints). Remaining gaps recorded in ADR 0059: per-child model overrides, JSON Schema *data* validation for structured children, and a dedicated workflow event type. | L |

| C13 | Review/guardian modes (second-model adversarial review of diffs/decisions) | codex review/guardian; DSH adversarial verification | Post-run middleware spawning a review subagent over `workspace.changed` paths + the turn-diff stream (A18) | M |
| C16 | Image input + view_image tool; reasoning effort/summary with encrypted replay | codex view_image + reasoning support | `model.Message` parts for images; adapter passthrough; reasoning items stored encrypted in run state and replayed verbatim | M |
| C19 | Webhook/schedule triggers and slash commands | DSH webhook/schedule, commands | Server endpoints creating runs from signed webhooks; cron scheduler reusing RunManager; commands as canned tasks | M |
| C20 | MCP server mode (expose ZenForge runs as an MCP server), elicitation, resources; MCP tool namespacing + read-only auto-approve | DSH MCP server; codex MCP namespaces/auto-approve | `adapters/mcp/server.go`: a transport-agnostic MCP **server** (`Serve` over any reader/writer, `Handle` for one raw message) with `initialize` version negotiation (a known version is echoed, an unknown one falls back to the newest rather than refusing the handshake), `ping`, `tools/list` (with `readOnlyHint` annotations and a default object schema), and `tools/call`, where a handler error is a result with `isError` and a malformed request/unknown method/unknown tool is the protocol's JSON-RPC code, a request without an id is a notification that is answered with nothing, a string id is echoed verbatim, and a panicking tool fails the call instead of killing the stream. Writing the server fixed the client's framing: it wrote LSP-style `Content-Length` headers, while MCP over stdio is newline-delimited JSON — the writer now emits the spec form and the reader still accepts headers (a cosmetic difference is not worth failing a connection over, but writing headers breaks every conforming server). ADR 0053. Client-side namespacing and annotations: `ServerOptions{Server,Deferred}` namespaces every remote tool as `mcp__<server>__<tool>` (a single underscore is legal in a tool name, so the double underscore is what makes the namespace unambiguous), sanitizes names onto the `[A-Za-z0-9_-]` grammar and keeps them within the 64-character limit with a deterministic hash suffix on truncation, refuses two remote names that would collapse onto one exposed name (which would silently hide a tool), keeps `Name()` (model-visible) apart from `RemoteName()` (what the server is called with), and reads `annotations.readOnlyHint`/`destructiveHint` into `ToolDefinition.ReadOnly()`/`Destructive()` — an absent hint is never read-only, and the call metadata carries `server`/`tool`/`readOnly` for the approval layer. CLI wiring: `zenforge mcp-server` serves the protocol over stdio with `zenforge_runs` (lists recorded runs, newest first, bounded by `limit`) and `zenforge_version`, both `readOnlyHint: true` — a tool call arrives with no approval prompt in front of it, so the server exposes only what it can offer without an operator watching, and a run-starting tool waits until the approval path is wired. Client wiring (ADR 0054): the config gains a top-level `mcpServers` map (`command`, `args`, `env`, `deferred`), validated before anything is spawned — a missing command, a name with surrounding whitespace or containing `__` (the namespace separator), or an unusable environment name fails configuration loading — and started in name order, so the same file registers the same tools in the same order. `buildAgent` starts each `StdioClient`, calls `initialize` and `tools/list` under a 30-second bound and `ToolsWithOptions(Server, Deferred, ToolCallTimeout)`, appends the tools before the `tool_search` decision (a `deferred` server is what makes lazy loading necessary), and refuses a cross-server exposed-name collision at startup instead of letting the registry fail mid-run; a server that cannot start fails the command, because this file is the operator's own, like the hook file, and a run that silently lacks the tool set the file asked for is worse than a loud failure (the reference logs and continues, and its default is recorded here as a deliberate deviation). The server process inherits the ambient environment minus credential-shaped names (`KEY|PASSWORD|SECRET|TOKEN`, the reference's heuristic) plus the entry's explicit `env`, so the provider key does not leak into a third-party process. Lifecycle: `options` carries named closers, `buildAgent` registers each client — and the event and checkpoint stores, which were opened and never closed before — the moment it opens it, and every command drains them on the way out, with the schedule loop draining per firing so a repeated schedule cannot accumulate server processes. Approval (default on; `ServerOptions.SkipApproval` is the explicit opt-out) ports the reference's rule exactly — destructive asks, read-only runs unattended, everything else asks unless the server declared the tool both non-destructive and closed-world, and an absent hint asks — and the request payload carries `server`/`tool`/`readOnly`/`destructive`/`openWorld` plus a rule key (`mcp:<server>:<tool>`) and an argument-covering fingerprint, so "always allow this tool" is per-tool while a run-scoped grant is per-payload. ADR 0055 makes the transport honest: one goroutine dispatches responses by id, so a context expiry abandons its own call instead of blocking the reader and a late response is dropped; a server-initiated frame is ignored rather than mistaken for a response or refused from the reader (which could deadlock against a peer still writing its request); `ToolCallTimeout` travels as the tool's declared `TimeoutDeclarer` budget (60 seconds from the CLI, the reference's default) and is armed by the existing timeout policy; and `Close` releases waiting calls and waits for the reader. The run-starting tool (ADR 0056): `zenforge mcp-server --allow-run` adds `zenforge_run` — the tool does not exist without the grant, so the operator's flag is the capability list and the protocol's unknown-tool error is the answer to an ungranted call. The tool is advertised without `readOnlyHint`, so a conforming client asks *its* own operator, while the grant protects *this* host; the served run is configured entirely by the operator's flags (workspace, tools, sandbox, hooks, checkpoint store — `mcp-server` now binds the standard option set, which it previously did not) and the caller supplies only the prompt; and the run's own approval-required tool calls follow `--approve`: `always` allows them, `never` refuses by choice, and `prompt` — unusable on a stdio server, where the interactive broker would read the protocol stream — becomes a refusal through an `options.approvalOverride` recording deny broker whose reason names the flag to change. Refused calls are reported to the model (which adapts) and to the caller with the run's answer, because a quietly hobbled run is worse than one that says what it refused; a failed, timed-out, or cancelled run is an `isError` result that still carries the run id (`--run-timeout`, default 15 minutes, bounds one run because the server answers requests in order, and a single run slot keeps an embedding host that drives `Handle` concurrently from overlapping two runs on one agent). Making cancellation real here also fixed a pre-existing defect the timeout path exposed: a JSONL checkpoint save that landed and then reported the context error left the loop's sequence counter behind the store, so the terminal cancellation checkpoint collided and the run reported a sequence conflict instead of the cancellation — the store now completes a durably-pending save (and a recovery of one) under a context that cannot be cancelled, and the loop re-derives its counter from the store after a failed save. Not ported: `resources`/`prompts`/elicitation/sampling, `listChanged` and progress notifications, a detached-run registry plus a status tool (the durable log is how a caller that cannot wait recovers a run), persistent cross-run MCP approval grants from the CLI (`Config.ApprovalGrants` is still SDK-only), per-server `startupTimeout`/`toolCallTimeout` configuration (both are constants), and server-initiated request handling (it needs a write path that does not run on the reader goroutine) | L |
| C23 | Memories (cross-run distilled learnings) | codex memories; DSH cross-session | `adapters/memory` extension: run-end distillation into scoped entries injected as instructions (A7 channel) | M |

## Verification

Everything marked **Shipped** is covered by Go tests runnable with:

```sh
env GOTOOLCHAIN=local go test ./...
```

Key suites: `compaction/`, `modelretry/`, `instructions/`, `diff/`,
`sessiontitle/`, `prompt/`, `tools/askuser/`, `tools/toolsearch/`,
`configlayer/`, `redact/`, `applypatch/`, `tools/patch/`, `web/`,
`tool/` (`planmode.go`), `tools/plan/`, `goals/` (goal lifecycle, round driver, Ralph), `tools/goal/`, `sandbox/seatbelt/` (macOS Seatbelt policy + adapter), `sandbox/bwrap/` (Linux bubblewrap layout + adapter), `checkpoint/` (`LoadAt` time travel), `eventlog/jsonl/` (tail cache,
concurrent writers), `cli/` (`fork`, `revert`, `resume --revert-to`),
`tools/web/` (SSRF-blocked targets, same-origin redirects, framing and
truncation, query bounds, search parsing),
`cli/` (`exec` protocol, config layers), `tools/contextinfo/` and
`tools/present/` (via root agent tests), `tools/shell/`,
`tools/workspace/` (glob, grep caps, turn-diff store, observation
policy), `tool/` (spill store + repeat guard), `approval/cli/`, and the
root-package agent tests (`compaction_agent_test.go`,
`retry_agent_test.go`, `context_agent_test.go`,
`context_meter_test.go`, `turn_diff_test.go`,
`environment_update_test.go`, `session_metadata_test.go`,
`prompt_registry_test.go`, `tool_search_agent_test.go`).
Docs claims are policed by `docs/links_test.go`,
`docs/schema_versions_test.go`, and `docs/mvp_validation_test.go`.
