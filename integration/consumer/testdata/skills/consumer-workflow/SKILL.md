---
name: consumer-workflow
description: Run the consumer lookup and sandbox verification workflow.
license: Apache-2.0
---
# Consumer workflow

Call the typed `lookup` tool for the answer, then call `shell` to verify the
sandbox operating system. Report both results.
