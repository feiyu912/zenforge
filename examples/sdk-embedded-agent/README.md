# SDK Embedded Agent Example

This example shows ZenForge embedded as a Go library without requiring an API
key. It uses:

- a local scripted model;
- a typed Go tool;
- in-memory event log and checkpoint stores;
- a redacted trace sink.

Run it with:

```bash
go run ./examples/sdk-embedded-agent
```
